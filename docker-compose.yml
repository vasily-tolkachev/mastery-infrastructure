services:
  postgres:
    image: postgres:17-alpine
    container_name: mastery-postgres
    restart: unless-stopped

    ports:
      - "5432:5432"

    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mastery}
      POSTGRES_USER: ${POSTGRES_USER:-mastery}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mastery}
      POSTGRES_INITDB_ARGS: "--auth=scram-sha-256"
      TZ: UTC

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mastery} -d ${POSTGRES_DB:-mastery}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

    networks:
      - mastery-net

  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/mastery/mastery-frontend:latest}
    pull_policy: always
    container_name: mastery-frontend
    restart: unless-stopped
    environment:
      PUBLIC_API_BASE_URL: ${PUBLIC_API_BASE_URL:-/api}
      PUBLIC_AUTH_BASE_URL: ${PUBLIC_AUTH_BASE_URL:-/api/auth}
    networks:
      - mastery-net

  nginx:
    image: nginx:1.27-alpine
    container_name: mastery-nginx
    restart: unless-stopped
    depends_on:
      - frontend
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.local.conf:/etc/nginx/nginx.conf:ro
    networks:
      - mastery-net

volumes:
  postgres_data:

networks:
  mastery-net:
